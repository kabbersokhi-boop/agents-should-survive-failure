# Contributing

Install prerequisites (Python 3.12, uv, Docker), run `make setup`, and verify changes with
`make verify`. Use conventional commits and include tests and documentation for behavior changes.
Never commit credentials or real vendor information.
