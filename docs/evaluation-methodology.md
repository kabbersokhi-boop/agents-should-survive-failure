# Evaluation Methodology

The current evaluator persists deterministic vendor-onboarding expectations and is included in the
local release command. It verifies one synthetic jurisdiction-risk behavior contract and records
machine-readable result rows. It does not execute a real workflow, simulate providers or tools, or
measure production reliability. A future evaluator must execute isolated real workflows and retain
actual failures rather than translating them into a perfect score.
